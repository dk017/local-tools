{
  "name": "offline-tools-tests",
  "version": "1.0.0",
  "description": "Integration test suite for offline-tools",
  "scripts": {
    "test": "npx playwright test",
    "test:web": "npx playwright test --config=playwright.config.ts",
    "test:web:local": "npx playwright test --config=playwright.config.local.ts",
    "test:desktop": "npx playwright test --config=playwright.desktop.config.ts",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug",
    "test:headed": "npx playwright test --headed",
    "test:update-snapshots": "npx playwright test --update-snapshots",
    "test:report": "npx playwright show-report playwright-report",
    "install:browsers": "npx playwright install",
    "install:deps": "npx playwright install-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "pdf-lib": "^1.17.1",
    "sharp": "^0.33.0",
    "adm-zip": "^0.5.10",
    "@types/adm-zip": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

