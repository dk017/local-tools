# Packaging & Distribution Task List

- [x] **Localization**: Ensure Image/PDF tools are localized.
- [x] **Backend**: Create Python API with FastAPI (`api.py`).
- [x] **Backend Build**: Create PyInstaller script (`build_backend.py`) and build binary.
- [x] **Tauri Sidecar**: Configure `tauri.conf.json` and `lib.rs` to use the sidecar.
- [x] **Frontend Web Mode**: `usePython` hook abstraction for Web vs Desktop.
- [x] **Frontend Routing**: Programmatic SEO with `react-router-dom` and `tools.json`.
- [x] **SEO**: Add `react-helmet-async` for meta tags.
- [ ] **Desktop Build**: Run `tauri build` to create installer.
- [ ] **Web Build**: Run `vite build` for web deployment (already verified).
- [ ] **Verification**: Ensure sidecar runs correctly in built app (User to verify).
